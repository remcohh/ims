<%= simple_form_for([@risk_register, @risk_mitigation]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :mitigation_step %><br>
  </div>
  
  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
