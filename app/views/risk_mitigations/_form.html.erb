<%= form_for(@risk_mitigation) do |f| %>
  <% if @risk_mitigation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@risk_mitigation.errors.count, "error") %> prohibited this risk_mitigation from being saved:</h2>

      <ul>
      <% @risk_mitigation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :risk_register_id %><br>
    <%= f.text_field :risk_register_id %>
  </div>
  <div class="field">
    <%= f.label :mitigation_step %><br>
    <%= f.text_area :mitigation_step %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
