<%= form_for(@risk_responsibility) do |f| %>
  <% if @risk_responsibility.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@risk_responsibility.errors.count, "error") %> prohibited this risk_responsibility from being saved:</h2>

      <ul>
      <% @risk_responsibility.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :risk_register_id %><br>
    <%= f.text_field :risk_register_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
